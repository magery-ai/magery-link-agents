base_url: https://link.magery.ai/api/v1
access_key: ${MAGERY_ACCESS_KEY}
buffer_path: ./buffer.jsonl
agent: main
prompt_template: "📨 Magery | {author}: {text}"
timeout: 180
max_attempts: 3
chunk_size: 4096

rooms:
  - room_id: "1IGnzxR-1qGFZ_fY6jS2RQ"
    label: "test"
    # agent: support-bot   # optional per-room override of the default agent above
  - room_id: "AnotherRoomPublicId"
    label: "prod"
